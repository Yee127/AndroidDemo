<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studydemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyDemo">
        <activity
            android:name=".state.todoDemo.a.ToDoMainActivity"
            android:exported="true"
            android:label="to_do_main"
            android:theme="@style/Theme.StudyDemo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".customize.CustomizeMainActivity"
            android:exported="true"
            android:label="customize"
            android:theme="@style/Theme.StudyDemo" />
        <activity
            android:name=".demo1.MainActivityOfDemo1"
            android:exported="true"
            android:label="@string/title_activity_card_main"
            android:theme="@style/Theme.StudyDemo" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.StudyDemo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>